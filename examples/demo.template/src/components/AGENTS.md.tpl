## Component conventions

- One component per file; filename matches the export.
- Co-locate tests next to the component (`Foo.tsx` + `Foo.test.tsx`).
- Avoid default exports — they make refactors noisier.
